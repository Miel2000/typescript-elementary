let friends: (string | number)[] = ["Alexis","Justine","Sophie"];
friends = [43,'EZEZ'];

let identity: (number | string)[] = ["Alex",13];
identity = ['Aloïa', 24];

let userData: { 
  name:string;
  age:number;
  isMajeur:true; 
  } = {  
    name: "Jean louis",
    age:42,
    isMajeur:true

 }

 console.log(userData);

 let yeetUser: {
   name:string;
   age:number;
   majeur:boolean;
 } = {
   name: "frensoi",
   age: 444,
   majeur:false
 }
 console.log(yeetUser);
